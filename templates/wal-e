{{ wal_e_cron_timespec }}   {{ wal_e_postgres_user }}   bash -c "/usr/bin/chpst -e /etc/wal-e.d/env /usr/local/bin/wal-e backup-push {{ wal_e_postgres_db_location }} && /usr/bin/chpst -e /etc/wal-e.d/env /usr/local/bin/wal-e delete --confirm retain {{ wal_e_retain_number }}"
